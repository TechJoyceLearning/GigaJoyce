# Classes (Structs)

## Module
Represents a loaded feature package with metadata, interface, settings and command maps.

## Guild
Wrapper around a Discord guild and its database document with resolved settings.

## Member
Wrapper around a Discord member, its guild, DB profile and resolved user settings.

## Command / SlashCommand / Subcommand / CommandHelp
Helper structures exported by modules and consumed by CommandHandler.
